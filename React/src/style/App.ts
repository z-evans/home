import styled from "styled-components"

export const RobloxTable = styled.table`
  border-spacing:0; 
  border-collapse: collapse;
`
export const StyledTableHead = styled.thead`
  background: #333;

  th {
    padding: 1em;
    margin: 0;
  }
`
export const StyledTableBody = styled.tbody`

  td {
    padding: 0.5em;
    margin: 0;
  }
`